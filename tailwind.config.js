/** @type {import('tailwindcss').Config} */
module.exports = {
    darkMode: 'class',
    // In Tailwind v4, use @source in your CSS file instead of content here
    theme: {
        extend: {},
    },
    plugins: [],
}
